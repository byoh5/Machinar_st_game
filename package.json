{
  "name": "machinar-st-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy:pages": "bash scripts/publish-github-pages.sh",
    "backgrounds:apply-photo": "bash scripts/apply-photo-backgrounds.sh",
    "backgrounds:check-photo": "bash scripts/apply-photo-backgrounds.sh --dry-run"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
